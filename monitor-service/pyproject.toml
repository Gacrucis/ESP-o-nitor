[project]
name = "esp-o-nitor-service"
version = "0.1.0"
description = "Local service that feeds the ESP-o-nitor for Claude/Codex usage"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "watchdog==6.0.0",
    "pillow==11.0.0",
]

[tool.pyright]
typeCheckingMode = "strict"

