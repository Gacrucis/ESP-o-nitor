# Local credentials (WiFi, OTA): NEVER version. See the *.example.* files
secrets.h
platformio_override.ini

# PlatformIO build artifacts (firmware/*/.pio)
.pio/

# Local PlatformIO toolchain and environment (firmware/*/.venv, .platformio-core)
.platformio-core/
.venv/

# Local tooling artifacts
.playwright-mcp/
.claude/
.DS_Store

# Design mockups and screenshots (not firmware assets)
*.png
# Exception: the 3D part previews are repo assets
!3d-models/previews/*.png
